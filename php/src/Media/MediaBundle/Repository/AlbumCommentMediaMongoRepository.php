<?php

namespace Media\MediaBundle\Repository;

use Doctrine\ODM\MongoDB\DocumentRepository;

/**
 * AlbumCommentMediaMongoRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class AlbumCommentMediaMongoRepository extends DocumentRepository
{
    /**
     * publish media for a comment
     * @param array $media_id
     * @return boolean
     */
    public function publishUserAlbumCommentMediaImage($media_id){
            $this->createQueryBuilder('UserAlbumCommentsMedia')
                    ->update()
                    ->multiple(true)
                    ->field('is_active')->set(1)
                    ->field('id')->in($media_id)
                    ->getQuery()
                    ->execute();
            
          return true;
    }
    
    public function getUserAlbumCommentMedia($commentsId) {
        $results = array();
        $qb = $this->createQueryBuilder();
        $results = $qb->field('comment_id')->in($commentsId)
                ->field('is_active')->equals(1)             
                ->getQuery()
                ->execute()
                ->toArray(false);
        return $results;
    }
    
     /**
     * removing the comment media data
     * @param type $comment_id
     * @return boolean
     */
    public function removeUserAlbumCommentsMedia($comment_id)
    {
            $qb = $this->createQueryBuilder();
            $qb->remove()
                ->field('comment_id')->equals($comment_id)
                ->getQuery()
                ->execute();
            return true;
    }
}