<?php

namespace Utility\MasterDataBundle\Repository;

use Doctrine\ORM\EntityRepository;

/**
 * MonthsListRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class MonthsListRepository extends EntityRepository
{
    /**
    * Privacy Setting List
    * @return object array
    */
    public function getMonthList($langCode){ 
        try{
        //object of query builder.
        $qb = $this->createQueryBuilder('c');
        $query = $qb->select('c.id, code.monthName as language_string')
                ->innerJoin('MasterDataBundle:MonthsListCodeByLang', 'code', 'WITH', 'code.monthCode = c.name')
                ->where('code.langCode =:langCode','c.status =:status')
                ->setParameter('langCode',$langCode )
                ->setParameter('status',1)
                ->getQuery();
        $response = $query->getResult();
        return $response;
        }catch(\Exception $e){
            return array();
        }
    }
}
